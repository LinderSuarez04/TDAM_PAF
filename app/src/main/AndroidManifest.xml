<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avn"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReservaFragment"
            android:theme="@style/AppTheme.NoActionBar"/>
            <!-- Puedes agregar otras configuraciones de actividad aquí -->

        <activity android:name=".PerfilFragment"
            android:theme="@style/AppTheme.NoActionBar"/>


        <activity android:name=".Activity_usuario"
            android:theme="@style/AppTheme.NoActionBar"/>



        <activity android:name=".TiendaFragment">
            <!-- Puedes agregar otras configuraciones de actividad aquí -->
        </activity>
        <activity android:name=".DetallesReservaActivity">
            <!-- Puedes agregar otras configuraciones de actividad aquí -->
        </activity>




        <activity
            android:name=".ReservaActivity"
            android:label="Tienda Activity">
            <!-- Otros atributos de la actividad, si los tienes -->
        </activity>



    </application>

</manifest>